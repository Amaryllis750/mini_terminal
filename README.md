# mini_terminal
A mini terminal that unfortunately isn't POSIX compliant

# Tests
The `test` bin can be created with `make test`.

The `test` executable creates a json object that represents our AST.
unfortunately, the test file doesn't completely follow JSON syntax and therefore your output JSON will have to be fixed (just ask some AI model to do fix the JSON)
